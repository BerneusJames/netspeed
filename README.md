To use this tool, first install the required library by running the following command in your terminal:
pip install speedtest-cli

Then run it with:
python netspeed.py

This will measure your internet speed by connecting to the nearest Speedtest server.
